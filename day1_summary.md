# Day 1 学习总结

## 今日题目

| 题号 | LeetCode | 题目 | 难度 | 核心技巧 |
|------|----------|------|------|----------|
| H01 | 1 | Two Sum（两数之和） | Easy | 哈希表 |
| H02 | 2 | Add Two Numbers（两数相加） | Medium | 链表 + 进位模拟 |

---

## H01 - Two Sum（两数之和）

### 问题

在数组中找出和为 target 的两个数，返回它们的下标。每个输入只有一个答案，不能重复使用同一元素。

### 解法：哈希表（一轮遍历）

- 遍历数组，对当前数字 `num`，计算需要的配对数字 `target - num`
- 检查这个配对数字是否已经在哈希表中出现过
- 出现过 → 直接返回答案；没出现 → 将当前数字和下标存入哈希表，继续遍历
- 时间复杂度 **O(n)**，空间复杂度 **O(n)**

### 关键点

- 暴力解法是两层循环 O(n²)，但利用哈希表的 O(1) 查找可以将复杂度降到 O(n)
- 只遍历一轮即可：因为 `a + b = target`，遍历到 b 时 a 一定已经存进去了
- 哈希表**存的是值到下标的映射**，需要返回下标而不是值本身

---

## H02 - Add Two Numbers（两数相加）

### 问题

两个非空链表，各自表示一个逆序存储的非负整数（每个节点存一位数字）。将两个数相加，返回同样格式的和链表。

### 解法：模拟竖式加法

- 链表天生逆序（头=个位），和手写竖式加法的顺序一致
- 同时遍历两个链表，对应位相加并加上上一位的进位 `carry`
- 用 `total % 10` 取当前位，用 `total // 10` 计算进位
- 用**哨兵节点（dummy node）**简化头节点的处理，避免对空链表的特殊判断
- while 循环条件必须包含 `carry`：即使两个链表都空了，如果还有进位需要额外生成一个节点（如 999 + 1 = 1000）
- 时间复杂度 **O(max(m, n))**，空间复杂度 **O(1)**（不计返回结果）

### 关键点

- `carry` 只会是 0 或 1，因为最大是 9 + 9 + 1 = 19
- 哨兵节点模式：`dummy = ListNode()` → `cur = dummy` → 最后 `return dummy.next`
- 空链表处理：用 `val1 = l1.val if l1 else 0` 统一逻辑，不等长的链表也能正常计算

---

## Python 技巧：`enumerate()`

在 H01 的代码中我们用到了 `enumerate(nums)`：

```python
for i, num in enumerate(nums):
    ...
```

### 它做了什么？

`enumerate()` 在遍历可迭代对象时，**同时给出索引和值**，省去手动维护计数器的麻烦。

### 对比：不用 vs 用 enumerate

```python
# ❌ 不用 enumerate：需要手动维护下标
i = 0
for num in nums:
    print(i, num)
    i += 1

# ❌ 用 range(len(...))：需要多写一层索引访问
for i in range(len(nums)):
    num = nums[i]
    print(i, num)

# ✅ 用 enumerate：一行拿到索引和值
for i, num in enumerate(nums):
    print(i, num)
```

### 指定起始索引

`enumerate` 默认从 0 开始计数，可以通过第二个参数指定起始值：

```python
for i, num in enumerate(nums, start=1):
    print(f"第{i}个数: {num}")  # 输出：第1个数: xxx, 第2个数: xxx...
```

### 在算法题中的应用场景

凡是需要**值 + 下标**的问题，`enumerate` 都是最干净的写法：

- Two Sum：需要记录值和下标，存到哈希表
- 找特定元素的位置
- 需要对比当前值和前一个/后一个值（配合下标访问相邻元素）

---

## 今日收获

1. **哈希表的空间换时间**：查找问题中，如果能建立"值→位置"的映射，可以从 O(n²) 降到 O(n)
2. **哨兵节点（dummy node）**：链表操作中非常实用的技巧，可以避免对头节点的特殊处理
3. **进位模拟**：凡是涉及加法（链表、字符串、数组）的问题，核心就是维护一个 `carry` 变量，用 `% 10` 取当前位，用 `// 10` 算进位